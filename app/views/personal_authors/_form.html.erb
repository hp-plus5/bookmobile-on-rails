<%= form_with(model: personal_author) do |form| %>
  <% if personal_author.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(personal_author.errors.count, "error") %> prohibited this personal_author from being saved:</h2>

      <ul>
        <% personal_author.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
