<%= form_with(model: personal_book) do |form| %>
  <% if personal_book.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(personal_book.errors.count, "error") %> prohibited this personal_book from being saved:</h2>

      <ul>
        <% personal_book.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
